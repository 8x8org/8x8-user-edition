name: Android User Edition V50 APK
on:
  push:
    branches: [main, 'release/**']
    paths:
      - 'android-app/**'
      - '.github/workflows/android-apk-v50.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Build installable development APK and release-unsigned APK
        run: |
          gradle -p android-app --no-daemon clean assembleDebug assembleRelease
      - name: Record artifact truth
        run: |
          mkdir -p android-dist
          cp android-app/app/build/outputs/apk/debug/app-debug.apk android-dist/8x8-user-edition-v50-0.2.0-dev-signed.apk
          cp android-app/app/build/outputs/apk/release/app-release-unsigned.apk android-dist/8x8-user-edition-v50-0.2.0-release-unsigned.apk
          sha256sum android-dist/*.apk | tee android-dist/SHA256SUMS.txt
          printf '%s\n' 'DEVELOPMENT_APK=INSTALLABLE_DEBUG_SIGNED' 'PRODUCTION_APK=NOT_SIGNED_NOT_PUBLISHED_AS_PRODUCTION' > android-dist/RELEASE_TRUTH.txt
      - uses: actions/upload-artifact@v4
        with:
          name: 8x8-user-edition-v50-apk
          path: android-dist/
          if-no-files-found: error
