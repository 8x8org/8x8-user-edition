# 8x8 OS Omniversal Command Atlas R2 — Release Unit

**Product maturity:** 0.0.1 Beta  
**Reality:** PUBLIC_PRESENT after exact-head merge and verified deployment  
**Implementation generation:** Command Atlas R2  
**Canonical source:** `8x8org/8x8-user-edition`

## Why this release exists

The prior public interface correctly exposed the Fabric architecture but remained visually too close to a documentation surface. The 2026-08-08 visual council review requires a stronger sovereign command-center language: dense instrumentation, a dominant red 8x8 core, compact operator panels, agent-council presence, world/system/evidence zoom, HUD geometry and deliberate mobile cockpit behavior.

## Release changes

- persistent operator rail on desktop and bottom cockpit rail on mobile;
- large red 8x8 core identity anchor;
- denser Omniversal Fabric Atlas;
- public-safe agent council and knowledge-assimilation section;
- Reality / Proof Graph rendered as a first-class system surface;
- explicit Sovereign, Intelligence, Continuity, Execution, Experience and Reality Fabric tiles;
- command deck, First Blink, Art Board 360, Studio, model-router and evidence surfaces;
- Three Realities rendered as structural state rather than marketing labels;
- visual-batch registry with SHA-256 provenance;
- explicit guard against copying concept-art telemetry as live metrics;
- accessibility-friendly reduced-motion and responsive layouts.

## Release gates

A production promotion is valid only when:

1. exact-head Public Information Boundary passes;
2. exact-head CodeQL passes;
3. canonical User Edition PR is merged;
4. the historical Vercel carrier mirrors the exact canonical artifact rather than an older design;
5. a READY deployment exists;
6. production alias serves HTML inline with product version `0.0.1 Beta` and the `Omniversal Command Atlas` marker;
7. `/`, `/first-blink`, `/world`, and `/art-board` resolve as browser-renderable HTML;
8. a deployment receipt records canonical source commit, carrier commit, deployment identity, production alias and rollback candidate.

## Truth boundary

This release improves visual density and system legibility. It does not make concept-image statistics real, expose protected topology, grant financial authority, certify the entire private fleet, or promote the whole future roadmap to Present.
