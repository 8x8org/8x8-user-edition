# MSG197 Completion Matrix V2

## Scope

This matrix records the evidence-backed state of the thirteen external capability candidates. A merged packet means the candidate has an exact pin, classification, decision, boundaries, hashes, tests or test design, and rollback doctrine. It does **not** mean the third-party runtime was installed into 8x8.

## Candidate decisions

| Candidate | Decision | Packet | Runtime |
|---|---|---|---|
| system-design-primer | Curated architecture knowledge | Merged | Not installed |
| TencentDB Agent Memory | Adopt schema and ACL patterns only | Merged | Synthetic canary owner-gated |
| agent-skills | Adopt workflow schemas, not skill execution | Merged | Not installed |
| superpowers | Adopt bounded patterns, reject orchestrator | Merged | Not installed |
| Uber ADR | Adopt detection telemetry schema and attack taxonomy only | Merged | No production telemetry or prevention |
| PDF Inspector | Retain bounded parser evidence; reject promotion | Merged, parser pass, supply chain blocked | Not installed |
| Supervision | Narrow model-agnostic subset on external node | Plan merged | Measured external evidence pending |
| Cloudflare Computer | Adopt workspace-boundary ideas, defer all execution | Merged | All backends denied |
| AirLLM | External CUDA only; phone and active node rejected | Plan merged | GPU evidence pending |
| LoopX | Extract selected protocols, defer runtime | Merged | No second control plane |
| DeepSeek-Reasonix | Extract selected extension protocols, defer runtime | Merged | No second agent runtime |
| Tailwind CSS | Defer without approved product requirement | Decision merged | Static CSS remains canonical |
| Next.js | Defer; static client remains canonical | Decision merged | No server runtime |

## PDF evidence

PR #71 merged at `a10d0fc3065a755fc1b8de4c0b2fe59f3664b2d8`.

The exact synthetic parser canary passed under a network-denied, read-only, unprivileged container with one CPU, 512 MiB RAM, 64 processes, and ten-second command limits. Promotion remains blocked by:

- `RUSTSEC-2026-0176`, affecting `pyo3 0.25.1`;
- `RUSTSEC-2026-0177`, affecting `pyo3 0.25.1`;
- `RUSTSEC-2026-0192`, marking `ttf-parser 0.25.1` unmaintained.

The packet is complete as an investigation. The candidate is not approved, installed, or production-ready.

## Corrected identities

- `uber/ADR` is **Agentic AI Detection and Response**, not an Architecture Decision Record generator.
- `cloudflare/computer` is a preview Durable Object virtual filesystem and execution substrate with filesystem, shell, JavaScript, and container backends. It is not primarily a browser-clicking agent.

## Real council state

The council receipt framework is merged. A valid vote requires a canonical agent identity, active lease, shared input digest, output digest, and receipt. ChatGPT supplied an advisory only. It is not counted as a vote. Current valid votes: **0**. Current quorum: **false**.

## Remaining evidence gates

1. Supervision requires a no-model external-node benchmark, SBOM, resource metrics, and uninstall proof.
2. AirLLM requires an approved CUDA node, licensed model selection, measured benchmark, and complete cache cleanup.
3. The Hermes-led council requires four real identity- and lease-bound votes plus owner acceptance.
4. PDF Inspector can be reconsidered only at a new immutable upstream pin with zero unresolved vulnerabilities and a maintained dependency chain.
5. Any candidate selected for implementation still needs a candidate-specific adapter branch, tests, security and license review, uninstall proof, owner approval, and release receipt.

## Absolute boundary

All thirteen candidate packets are merged. Zero third-party candidates are installed into the active 8x8 runtime. No Termux or Ubuntu PRoot service was restarted. No credential, wallet, private file, private document, model, camera, microphone, or production telemetry was used. No production deployment was performed by MSG197.
